<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="CviceniTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/cviceni_bg</item>
        <item name="android:colorBackground">@color/cviceni_bg</item>
        <item name="android:colorPrimary">@color/cviceni_header</item>
        <item name="android:colorPrimaryDark">@color/cviceni_header</item>
        <item name="android:navigationBarColor">@color/cviceni_header</item>
    </style>

</resources>
